# Copy this to .env and fill in your values

# URL of your FastAPI backend
# In development, leave empty to use Vite proxy (proxied via /api to localhost:8000)
# In production (Railway), set this to your backend URL:
VITE_API_URL=https://your-backend.railway.app/api/v1
